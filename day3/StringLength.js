let str = prompt("enter string value")
count = 0;
for (let i = 0; i < str.length; i++){
    count++
}
console.log(`length of ${str} is ${count}`);
document.write(str.length);
document.writeln(`length of ${str} is ${count}`);
